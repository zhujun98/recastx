syntax = "proto3";
package recastx;

import "reconstruction.proto";
import "state.proto";

message Message {
    oneof request {
        StatePacket state = 1;
        ReconRequestPacket recon = 2;
    }
}

