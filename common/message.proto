syntax = "proto3";
package recastx;

import "parameter.proto";
import "reconstruction.proto";
import "state.proto";

message Message {
    oneof request {
        StatePacket state = 1;
        ReconRequestPacket recon = 2;
        SetParameterPacket param = 3;
    }
}
